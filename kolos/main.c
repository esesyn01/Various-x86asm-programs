#include <stdio.h>
#include <Windows.h>
unsigned int nwd(unsigned int a, unsigned int b);
short fth(float a);
wchar_t* zami(unsigned int liczba);
char* spacje(char znak);
void* rozklad(unsigned __int64[], unsigned int);
unsigned int zamm(wchar_t* znaki);
int main()
{
	//printf("%d", fth(1.0));

	/*unsigned int n = 79342314;
	wchar_t* wyn = zami(n);
	MessageBoxW(0, wyn, wyn, 0);
	unsigned int w = zamm(wyn);
	printf("%d %d",n, w);
	if (w == n)
	{
		printf("Ako lepsze");
	}*/

	/*char* wyn = spacje('a');
	for (int i = 0; i < 129; i++)
	{
		printf("%c", wyn[i]);
	}*/

	/*int* tab;
	unsigned __int64 tabb[4] = { 0x0F,0x0A, 0x45671234 ,0x4232000A};
	tab = rozklad(tabb, 4);
	for (int i = 0; i < 16; i++)
	{
		printf("%d", tab[i]);
	}*/


	return 0;
}